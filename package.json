{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.0.0-rc.0",
    "prettier": "^1.13.7",
    "eslint": "^3.2.2",
    "@types/jest": "^23.1.0",
    "ts-jest": "^23.0.0",
    "jest": "^23.4.2"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build",
    "test": "npm run build && jest"
  }
}
